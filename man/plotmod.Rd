% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotmod.R
\name{plotmod}
\alias{plotmod}
\alias{plotmod.bestfit}
\alias{plotmod.lm}
\title{Plot bestfit models}
\usage{
plotmod(object, ...)

\method{plotmod}{bestfit}(object, fit = 1, interval = c("none",
  "confidence", "prediction"), level = 0.8, local, ...)

\method{plotmod}{lm}(object, interval = c("none", "confidence", "prediction"),
  level = 0.8, func = "identity", local, ...)
}
\arguments{
\item{object}{Object of class \code{\link{bestfit}}}

\item{\dots}{further arguments passed to predict.lm.}

\item{fit}{the number of the chosen fit from the combination matrix (defaults
for the best fit found with \code{\link{bestfit}}).}

\item{interval}{the type of interval calculation (provided to predict.lm) to 
be ploted with the model.}

\item{level}{Tolerance/confidence level (provided to predict.lm) to be
ploted.}
}
\description{
\code{plotmod} plots \code{\link{bestfit}} models
}
\examples{
best_fit <- bestfit(valor ~ .,  data = data)
plotmod(best_fit, interval = "confidence")
plotmod(best_fit, fit = 2, interval = "confidence")
plotmod(best_fit, interval = "confidence", 
        local = list(area_total = 205, quartos = 3, suites = 1, garagens = 2, dist_b_mar = 250, padrao = "medio"))

fit <- lm(log(valor) ~ ., data = data)
plotmod(fit, interval = "confidence")
plotmod(fit, interval = "confidence", func = "log")
plotmod(fit, interval = "confidence", 
        local = list(area_total = 205, quartos = 3, suites = 1, garagens = 2, dist_b_mar = 250, padrao = "medio"))
plotmod(fit, interval = "confidence", func = "log",
        local = list(area_total = 205, quartos = 3, suites = 1, garagens = 2, dist_b_mar = 250, padrao = "medio"))

}
